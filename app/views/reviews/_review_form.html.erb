<%= form_for [@product, @review], remote: true do |form| %>
  <%= form.label :body, "New Review:" %><br />
  <%= form.text_area :body, rows: 5, required: true, minlength: 25, maxlength: 300 %><br />
  <%= submit_tag "Submit" %>
<% end %>
