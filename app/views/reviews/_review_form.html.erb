<%= form_for [@product, @review], remote: true do |form| %>
  <%= form.text_area :body, rows: 5, placeholder: 'Write your review here...', required: true, maxlength: 500, class: 'form-control' %>
  <%= submit_tag "Submit", class: 'btn btn-primary' %>
<% end %>
