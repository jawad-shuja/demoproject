<%= form_for [@product, @review], remote: true do |form| %>
  <%= form.text_area :body, rows: 5, placeholder: 'Write your review here...', required: true, minlength: 5, maxlength: 300, class: 'form-control' %>
  <%= submit_tag "Submit", class: 'btn btn-primary' %>
<% end %>
