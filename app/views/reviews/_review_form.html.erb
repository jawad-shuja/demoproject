<%= form_for [@product, @review], remote: true do |form| %>
  <%= form.label :body, "New Review:" %>
  <%= form.text_area :body, rows: 5, required: true, minlength: 5, maxlength: 300, class: 'form-control' %>
  <%= submit_tag "Submit", class: 'btn btn-success' %>
<% end %>
