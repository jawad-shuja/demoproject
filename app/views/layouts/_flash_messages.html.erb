<div class="row">
  <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <% flash.each do |type, message| %>
      <div class="alert <%= flash_class_for(type) %>">
        <%= message %>
      </div>
    <% end %>
  </div>
</div>
